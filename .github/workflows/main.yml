name: Main Taski Workflow
on:
  push:
    branches:
      - main
jobs:
  checkout-and-print-latest
  staps:
    - name: Check out repository code
      uses: actions/checkout@v4
    - name: Print project tree
      run: tree .
